from flask import Flask, send_file

app = Flask(__name__)

@app.route('/validation-key.txt')
def validation_key():
    return send_file('validation-key.txt')

if __name__ == '__main__':
    app.run(debug=True
